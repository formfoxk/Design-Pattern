package account;

public class MinusWithdrawTransaction extends WithdrawTransaction{

	private double limitedMoney;
	
	public MinusWithdrawTransaction(double balance, double money, double limitedMoney) {
		// TODO Auto-generated constructor stub
		super(balance, money);
		this.limitedMoney = limitedMoney;
	}
	
	public double execute(){
		if((balance+limitedMoney)-money > 0)
			return 
	}
}
